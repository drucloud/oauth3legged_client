To-do

`. Have a .install file to insert all pre-set values to database
1. Save oauth tokens to user profile instead of variable table
3. Denied user if the UDID & uid not found in database
4. Summarize all variable_get into 1 request (require to change oauthclient.admin.inc)